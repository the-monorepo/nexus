import * as mbx from 'mobx-dom';
import { DomElement } from 'mobx-dom';

import { customElement } from 'custom-element';

import css from '';

@customElement('list-section')
export class ListSectionElement extends DomElement {
  render() {

  }
}

