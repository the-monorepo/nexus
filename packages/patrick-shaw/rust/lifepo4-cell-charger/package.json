{
  "name": "@pshaw/lifepo4-cell-charger",
  "version": "1.0.0",
  "description": "Arduino logic for Arduino part side of the spot welder app",
  "private": true,
  "scripts": {
    "list:current-avr-port": "bash -c 'ls /dev/cu.usbserial*'",
    "install:project:osx": "bash -c 'brew install avr-gcc && brew install arduino-cli && arduino-cli core update-index && arduino-cli core install arduino:avr && arduino-cli core install arduino:megaavr && arduino-cli lib install \"PMS Library\"'",
    "start": "yarn build && yarn upload",
    "build": "bash -c 'cargo -Z build-std=core build && avr-objcopy -O ihex ./target/avr-atmega2560/debug/lifepo4-cell-charger.elf ./release.hex'",
    "upload": "bash -c 'arduino-cli upload --input-file ./release.hex -p $(yarn list:current-avr-port) --fqbn arduino:avr:mega'"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/the-monorepo/monorepo.git",
    "directory": "pshaw/lifepo4-cell-charger"
  }
}
